#pragma once

/**
 * This class handle a rendering pass
 * 
 * TODO: It should ne possible to attach a framebuffer to the pass
 * instead of rendering to the default one.
 */

class Pass
{
public:
	Pass();
	~Pass();

private:

};
